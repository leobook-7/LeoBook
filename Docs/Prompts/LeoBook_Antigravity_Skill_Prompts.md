# LeoBook — Antigravity Skill Prompts Master Reference
> All prompts are designed for use in **Google Antigravity IDE** with the `antigravity-skills` library.
> Tag syntax: `@[skill-name]` — chain multiple for complex tasks.
> Project: [LeoBook v7.0](https://github.com/Materialless-LLC/LeoBook) · Materialless LLC

---

## Table of Contents

- [LeoBook — Antigravity Skill Prompts Master Reference](#leobook--antigravity-skill-prompts-master-reference)
  - [Table of Contents](#table-of-contents)
  - [1. Architecture \& Planning](#1-architecture--planning)
  - [2. Building New Features](#2-building-new-features)
  - [3. Debugging \& Fixing](#3-debugging--fixing)
  - [4. RL Model \& AI Engine](#4-rl-model--ai-engine)
  - [5. Flutter Frontend](#5-flutter-frontend)
  - [6. Code Review \& Branch Cleanup](#6-code-review--branch-cleanup)
  - [7. Documentation](#7-documentation)
  - [8. Parallel Development](#8-parallel-development)
  - [9. MCP \& Tool Integration](#9-mcp--tool-integration)
  - [10. Testing \& QA](#10-testing--qa)
  - [11. Database \& Supabase](#11-database--supabase)
  - [12. AIGO Self-Healing Pipeline](#12-aigo-self-healing-pipeline)
  - [13. Prediction Accuracy \& Analytics](#13-prediction-accuracy--analytics)
  - [14. Performance \& Scalability](#14-performance--scalability)
  - [15. Playwright \& Browser Automation](#15-playwright--browser-automation)
  - [16. Git \& Branch Strategy](#16-git--branch-strategy)
  - [17. Skill \& Workflow Extension](#17-skill--workflow-extension)
  - [18. Flutter UI Deep Work](#18-flutter-ui-deep-work)
  - [19. Security \& Environment](#19-security--environment)
  - [20. Live Streamer Isolation](#20-live-streamer-isolation)
  - [21. End-to-End Sprint (Most Powerful Combo)](#21-end-to-end-sprint-most-powerful-combo)
  - [22. Neural RL Architecture Deep Dive](#22-neural-rl-architecture-deep-dive)
  - [23. Betting Market Expansion](#23-betting-market-expansion)
  - [24. Multi-League Enrichment Optimization](#24-multi-league-enrichment-optimization)
  - [25. Prediction Confidence System](#25-prediction-confidence-system)
  - [26. Codebase Mapping \& Onboarding](#26-codebase-mapping--onboarding)
  - [27. Task Scheduler Hardening](#27-task-scheduler-hardening)
  - [28. Odds Movement Tracking](#28-odds-movement-tracking)
  - [29. Technical Debt Audit](#29-technical-debt-audit)
  - [30. Multi-Timezone Match Handling](#30-multi-timezone-match-handling)
  - [31. DevContainer \& CI/CD](#31-devcontainer--cicd)
  - [32. Flutter Real-Time Streaming](#32-flutter-real-time-streaming)
  - [33. Modular Plugin Architecture](#33-modular-plugin-architecture)
  - [34. Drawdown \& Bankroll Management](#34-drawdown--bankroll-management)
  - [35. LLM Enrichment Pipeline](#35-llm-enrichment-pipeline)
  - [36. Demo \& Presentation](#36-demo--presentation)
  - [37. Maximum Complexity Sprint (Ultimate Combo)](#37-maximum-complexity-sprint-ultimate-combo)
  - [38. Error \& Log Debugging](#38-error--log-debugging)
    - [Prologue Gate Failures](#prologue-gate-failures)
    - [Chapter 1 Odds Harvesting Errors](#chapter-1-odds-harvesting-errors)
    - [RL Adapter Readiness Crash](#rl-adapter-readiness-crash)
    - [Supabase Sync Errors](#supabase-sync-errors)
    - [Live Streamer Silent Death](#live-streamer-silent-death)
    - [Gemini Key Rotation Failures](#gemini-key-rotation-failures)
    - [Prediction Pipeline Wrong Output](#prediction-pipeline-wrong-output)
    - [Scheduler Drift \& Missed Tasks](#scheduler-drift--missed-tasks)
  - [39. Rulebook First Principles](#39-rulebook-first-principles)
    - [Apply RULEBOOK to New Feature Design](#apply-rulebook-to-new-feature-design)
    - [RULEBOOK Violation Audit](#rulebook-violation-audit)
    - [First Principles Refactor Decision](#first-principles-refactor-decision)
    - [RULEBOOK as Agent Constitution](#rulebook-as-agent-constitution)
    - [Engineering Decision Log](#engineering-decision-log)
  - [40. Modular Evaluation — Prologue](#40-modular-evaluation--prologue)
    - [Prologue Full Evaluation](#prologue-full-evaluation)
    - [Prologue P1 Deep Evaluation](#prologue-p1-deep-evaluation)
    - [Prologue P2 Deep Evaluation](#prologue-p2-deep-evaluation)
    - [Prologue P3 Deep Evaluation](#prologue-p3-deep-evaluation)
  - [41. Modular Evaluation — Startup](#41-modular-evaluation--startup)
    - [Startup Full Evaluation](#startup-full-evaluation)
    - [Startup DB Parity Deep Evaluation](#startup-db-parity-deep-evaluation)
  - [42. Modular Evaluation — Chapter 1](#42-modular-evaluation--chapter-1)
    - [Chapter 1 Full Evaluation](#chapter-1-full-evaluation)
    - [Chapter 1 P1 Deep Evaluation](#chapter-1-p1-deep-evaluation)
    - [Chapter 1 P2 Deep Evaluation](#chapter-1-p2-deep-evaluation)
    - [Chapter 1 P3 Deep Evaluation](#chapter-1-p3-deep-evaluation)
  - [43. Modular Evaluation — Chapter 2](#43-modular-evaluation--chapter-2)
    - [Chapter 2 Full Evaluation](#chapter-2-full-evaluation)
    - [Chapter 2 P1 Deep Evaluation](#chapter-2-p1-deep-evaluation)
    - [Chapter 2 P2 Deep Evaluation](#chapter-2-p2-deep-evaluation)
  - [44. Modular Evaluation — Live Streamer](#44-modular-evaluation--live-streamer)
    - [Streamer Full Evaluation](#streamer-full-evaluation)
    - [Streamer Outcome Review Evaluation](#streamer-outcome-review-evaluation)
    - [Streamer Accuracy Report Evaluation](#streamer-accuracy-report-evaluation)
  - [45. Modular Evaluation — Cross-Cutting](#45-modular-evaluation--cross-cutting)
    - [now\_ng Utilities Full Evaluation](#now_ng-utilities-full-evaluation)
    - [Config Layer Full Evaluation](#config-layer-full-evaluation)
    - [Data Flow Full Evaluation](#data-flow-full-evaluation)
  - [46. End-to-End Latency Evaluation](#46-end-to-end-latency-evaluation)

---

## 1. Architecture & Planning

```
@[brainstorming] @[writing-plans] @[bdi-mental-states]
Analyze the LeoBook v7.0 architecture (Leo.py orchestrator, Task Scheduler, Data Readiness Gates P1-P3, Neural RL engine). Identify the weakest architectural points and propose improvements for reliability, scalability, and prediction accuracy.
```

---

## 2. Building New Features

```
@[subagent-driven-development] @[test-driven-development] @[filesystem-context]
Design and implement a new LeoBook feature: Asian Handicap neural RL prediction support. Coordinate sub-agents to handle: (1) data schema changes in Supabase, (2) RL adapter training logic in Core/Intelligence/rl/, (3) integration into the prediction pipeline in Leo.py Chapter 1.
```

---

## 3. Debugging & Fixing

```
@[systematic-debugging] @[context-degradation] @[verification-before-completion]
Leo.py's AIGO self-healing extractor in Core/Browser/ is failing silently during Flashscore live score streaming. Systematically diagnose the root cause, fix it, and verify the fix with concrete evidence before marking complete.
```

---

## 4. RL Model & AI Engine

```
@[project-development] @[memory-systems] @[context-optimization]
Audit the Neural RL engine in Core/Intelligence/rl/ — SharedTrunk + LoRA league adapters + PPO training. Identify context window inefficiencies, propose memory/state optimizations, and design a better chronological walkthrough strategy for training.
```

---

## 5. Flutter Frontend

```
@[frontend-design] @[ui-ux-pro-max] @[web-artifacts-builder]
Redesign the LeoBook Flutter app's 4-tab match system with enhanced Liquid Glass UI. Improve the real-time Supabase streaming UX, add micro-animations for live score updates, and maintain the Telegram-grade high-density aesthetic.
```

---

## 6. Code Review & Branch Cleanup

```
@[requesting-code-review] @[finishing-a-development-branch] @[verification-before-completion]
Review the latest changes to Leo.py's Task Scheduler and Data Readiness Gate logic. Verify correctness of the "1 Prediction Per Team Per Week" constraint, check edge cases, then finalize the branch for merge.
```

---

## 7. Documentation

```
@[doc-coauthoring] @[writing-plans] @[internal-comms]
Update LeoBook's RULEBOOK.md and LeoBook_Technical_Master_Report.md to reflect all v7.0 architectural decisions: Autonomous Task Scheduler, computed Standings VIEW, Smart Prediction Scheduling, and RL adapter readiness gates. Commit and push to my github repo when done.
```

---

## 8. Parallel Development

```
@[dispatching-parallel-agents] @[multi-agent-patterns] @[executing-plans]
Dispatch parallel agents to simultaneously work on: (1) Weekly enrichment pipeline bug fixes in Scripts/enrich_leagues.py, (2) Supabase VIEW optimization in Data/Supabase/, (3) Flutter computed_standings query performance in leobookapp/. Coordinate outputs and merge without conflicts.
```

---

## 9. MCP & Tool Integration

```
@[mcp-builder] @[tool-design] @[project-development]
Build an MCP server for LeoBook that exposes: live prediction results, bet placement status, RL model confidence scores, and Data Readiness Gate states — so external tools and dashboards can query Leo.py's state without direct DB access.
```

---

## 10. Testing & QA

```
@[test-driven-development] @[webapp-testing] @[receiving-code-review]
Write a full test suite for Leo.py's Prologue (P1-P3 Data Readiness Gates). Cover: threshold failures, auto-remediation triggers, edge cases where leagues have 0 historical seasons, and RL adapter missing scenarios. Use Playwright for any browser-dependent betting automation tests.
```

---

## 11. Database & Supabase

```
@[systematic-debugging] @[context-optimization] @[verification-before-completion]
Audit all Supabase Postgres VIEW definitions in Data/Supabase/. Optimize the computed_standings UNION ALL view for query speed, identify any N+1 query patterns in Data/Access/, and verify the bi-directional cloud sync bootstrap logic handles network failures gracefully.
```

---

## 12. AIGO Self-Healing Pipeline

```
@[bdi-mental-states] @[memory-systems] @[systematic-debugging]
Model the AIGO self-healing extractor (Core/Browser/) using a Belief-Desire-Intention framework. Map what AIGO believes about page state, what it desires (successful extraction), and how its intentions (selector fallbacks, retries) should be structured. Then refactor the logic to be more robust and explainable.
```

---

## 13. Prediction Accuracy & Analytics

```
@[evaluation] @[advanced-evaluation] @[writing-plans]
Design a multi-dimensional evaluation system for LeoBook's prediction engine. Cover: per-market accuracy (1X2, BTTS, Over/Under, Asian Handicap), RL vs Rule Engine contribution breakdown, league-level performance variance, and a monthly accuracy report format compatible with the Flutter dashboard.
```

---

## 14. Performance & Scalability

```
@[context-compression] @[filesystem-context] @[context-optimization]
Profile Leo.py's full autonomous cycle for bottlenecks. Focus on: Gemini multi-key rotation latency, Playwright extraction timeouts, SQLite vs Supabase sync delays, and RL inference time per match. Propose a prioritized optimization plan with expected gains per fix.
```

---

## 15. Playwright & Browser Automation

```
@[systematic-debugging] @[webapp-testing] @[aigo-self-healing]
Harden the FootballCom betting automation module (Modules/FootballCom/). Add resilient selector strategies for login, odds harvesting, and bet booking flows. Implement screenshot-on-failure logging and auto-retry with exponential backoff for all Playwright interactions.
```

---

## 16. Git & Branch Strategy

```
@[using-git-worktrees] @[finishing-a-development-branch] @[multi-agent-patterns]
Set up a Git worktree strategy for LeoBook so that: RL training experiments run in an isolated worktree, Flutter frontend changes are developed in parallel without blocking backend work, and hotfixes to Leo.py can be applied independently. Define the branch naming convention and merge policy.
```

---

## 17. Skill & Workflow Extension

```
@[skill-creator] @[writing-skills] @[using-superpowers]
Create a custom LeoBook-specific skill called "leobook-orchestrator" that encodes: the v7.0 pipeline rules from RULEBOOK.md, the Data Readiness Gate sequence, Smart Prediction Scheduling constraints, and the correct chapter execution order — so any agent working on LeoBook automatically follows project standards.
```

---

## 18. Flutter UI Deep Work

```
@[ui-ux-pro-max] @[frontend-design] @[theme-factory]
Generate a complete LeoBook design system: Liquid Glass color palette, typography scale, micro-radius tokens (14dp), spacing system, and component library (match cards, prediction chips, odds badges, live score indicators). Output as a Flutter ThemeData + reusable widget spec.
```

---

## 19. Security & Environment

```
@[project-development] @[writing-plans] @[doc-coauthoring]
Audit LeoBook's environment variable handling (.env.example, GEMINI_API_KEY rotation, SUPABASE_SERVICE_KEY usage). Design a secrets management strategy that: prevents key leakage in logs, rotates Gemini keys on quota exhaustion gracefully, and documents safe deployment practices for the Codespace environment.
```

---

## 20. Live Streamer Isolation

```
@[dispatching-parallel-agents] @[context-fundamentals] @[systematic-debugging]
The Live Streamer (Modules/Flashscore/) runs as an isolated parallel task waiting for Bootstrap Sync. Redesign its lifecycle management: define clean startup/shutdown signals, handle Bootstrap Sync timeout gracefully, prevent memory leaks during long-running sessions, and add heartbeat logging.
```

---

## 21. End-to-End Sprint (Most Powerful Combo)

```
@[brainstorming] @[writing-plans] @[subagent-driven-development] @[verification-before-completion]
Plan and execute a full LeoBook sprint: start by brainstorming the top 5 highest-impact improvements across Leo.py, RL engine, Flutter app, and Supabase layer. Write a detailed execution plan with agent assignments. Execute each task in parallel using sub-agents. Verify every change with concrete test evidence before marking the sprint complete.
```

---

## 22. Neural RL Architecture Deep Dive

```
@[project-development] @[evaluation] @[context-optimization]
Conduct a full audit of the SharedTrunk + LoRA league adapter architecture in Core/Intelligence/rl/. Evaluate: adapter parameter efficiency per league, gradient interference between shared trunk and league-specific heads, PPO reward shaping for composite betting markets, and propose a next-gen architecture that handles cold-start leagues with zero historical data.
```

---

## 23. Betting Market Expansion

```
@[brainstorming] @[test-driven-development] @[writing-plans]
Design the full implementation plan for adding 3 new betting markets to LeoBook: (1) Anytime Goalscorer, (2) Half-Time/Full-Time, (3) Both Teams Score in Both Halves. Cover: data schema changes, rule engine logic, RL reward shaping, FootballCom odds scraping selectors, and Flutter UI display for each market.
```

---

## 24. Multi-League Enrichment Optimization

```
@[subagent-driven-development] @[dispatching-parallel-agents] @[systematic-debugging]
The weekly enrichment pipeline (Scripts/enrich_leagues.py) processes leagues sequentially. Redesign it using parallel sub-agents: assign leagues by region to isolated workers, implement a shared progress tracker in Supabase, add gap-scan deduplication so two agents never enrich the same league, and benchmark throughput improvement vs the current single-thread approach.
```

---

## 25. Prediction Confidence System

```
@[bdi-mental-states] @[advanced-evaluation] @[project-development]
Design a prediction confidence scoring system for LeoBook. For every prediction, compute: Rule Engine confidence (based on historical win rates), RL model entropy (uncertainty from PPO policy head), ensemble agreement score (when both agree vs diverge), and market-specific confidence thresholds. Surface these scores in the Flutter app with visual confidence indicators per bet.
```

---

## 26. Codebase Mapping & Onboarding

```
@[filesystem-context] @[doc-coauthoring] @[writing-plans]
Generate a complete LeoBook codebase onboarding guide for a new senior engineer. Map every file in Core/, Modules/, Scripts/, Data/, and leobookapp/ to its runtime role in the autonomous pipeline. Highlight the 10 most critical files a new dev must understand first, common pitfalls from RULEBOOK.md, and a day-1 setup checklist from scratch.
```

---

## 27. Task Scheduler Hardening

```
@[systematic-debugging] @[test-driven-development] @[verification-before-completion]
Stress-test Core/System/scheduler.py under edge cases: (1) server restart mid-task, (2) two tasks scheduled at identical timestamps, (3) Monday 2:26am weekly enrichment missed due to downtime, (4) day_before_predict tasks for matches with kick-off time changes. Fix all failure modes and add a scheduler audit log to Supabase for post-mortem analysis.
```

---

## 28. Odds Movement Tracking

```
@[brainstorming] @[project-development] @[mcp-builder]
Design an odds movement tracking subsystem for LeoBook. Capture odds snapshots at multiple intervals before match kick-off (24h, 6h, 1h, kick-off), store deltas in Supabase, compute line movement signals (sharp money indicators), and expose this data via an MCP tool so the RL engine can use odds drift as a real-time feature input.
```

---

## 29. Technical Debt Audit

```
@[requesting-code-review] @[context-degradation] @[writing-plans]
Perform a full technical debt audit of Leo.py (the main orchestrator file). Identify: functions exceeding 50 lines that should be decomposed, implicit state dependencies between chapters, hardcoded constants that belong in Config/, silent exception swallowing, and deprecated logic from pre-v7.0. Produce a prioritized refactor backlog sorted by risk vs effort.
```

---

## 30. Multi-Timezone Match Handling

```
@[systematic-debugging] @[test-driven-development] @[context-fundamentals]
LeoBook uses Core/Utils/now_ng utilities for time handling. Audit every timestamp operation across the codebase for timezone correctness. Cover: Flashscore match times (UTC vs local), Supabase datetime storage, scheduler task timing (Nigeria timezone vs server UTC), day_before_predict boundary calculations, and LEO_CYCLE_WAIT_HOURS drift over DST transitions.
```

---

## 31. DevContainer & CI/CD

```
@[project-development] @[writing-plans] @[verification-before-completion]
Redesign LeoBook's .devcontainer/setup.sh and Codespace environment for production-grade reliability. Add: dependency pinning for all pip packages, Playwright browser caching across container restarts, automatic .env validation on startup, a GitHub Actions workflow for RL model regression testing on every PR, and a nightly autonomous cycle smoke test.
```

---

## 32. Flutter Real-Time Streaming

```
@[frontend-design] @[ui-ux-pro-max] @[systematic-debugging]
Audit and harden the Flutter app's Supabase real-time streaming implementation. Fix: subscription lifecycle leaks on tab switches, race conditions between computed_standings VIEW refresh and UI render, 2.5hr status propagation edge cases for delayed matches, and add optimistic UI updates so the app feels instant even on slow connections.
```

---

## 33. Modular Plugin Architecture

```
@[multi-agent-patterns] @[tool-design] @[skill-creator]
Redesign LeoBook's Modules/ directory as a true plugin architecture. Each module (Flashscore, FootballCom, future bookmakers) should expose a standard interface: extract(), validate(), submit(), and report(). Leo.py should dynamically load modules at runtime. Design the interface spec, migration plan from current hardcoded modules, and a template for adding a new bookmaker in under 2 hours.
```

---

## 34. Drawdown & Bankroll Management

```
@[brainstorming] @[advanced-evaluation] @[writing-plans]
Design a bankroll management and drawdown protection system for LeoBook's betting automation. Implement: Kelly Criterion stake sizing per prediction confidence tier, maximum daily loss circuit breaker (halt betting if drawdown > X%), streak detection (pause after N consecutive losses), and a recovery mode that reduces stake size post-drawdown. Integrate with Chapter 2 and surface status in Flutter.
```

---

## 35. LLM Enrichment Pipeline

```
@[context-compression] @[memory-systems] @[project-development]
Audit Scripts/build_search_dict.py's LLM enrichment pipeline that uses Gemini multi-key rotation. Optimize: prompt design for maximum extraction accuracy per token, caching of previously enriched entities to avoid redundant API calls, fallback behavior when all 25+ Gemini keys hit quota simultaneously, and batch processing to reduce total enrichment time by at least 60%.
```

---

## 36. Demo & Presentation

```
@[canvas-design] @[doc-coauthoring] @[brand-guidelines]
Create a full LeoBook investor/demo package: (1) a 16:9 system architecture poster showing the v7.0 autonomous pipeline visually, (2) a one-page technical brief summarizing the Neural RL engine, Data Readiness Gates, and betting market coverage, (3) a Flutter app screenshot showcase with Liquid Glass UI highlights. Professional, production-grade output.
```

---

## 37. Maximum Complexity Sprint (Ultimate Combo)

```
@[brainstorming] @[subagent-driven-development] @[multi-agent-patterns] @[dispatching-parallel-agents] @[verification-before-completion]
Orchestrate a full LeoBook v8.0 planning session. Brainstorm every system that needs upgrading from v7.0. Assign specialized sub-agents: one for RL architecture evolution, one for Flutter UI overhaul, one for Supabase schema migration, one for betting automation hardening, one for DevContainer CI/CD modernization. Each agent produces a detailed spec. Synthesize all specs into a unified v8.0 roadmap with zero conflicts, dependency ordering, and milestone checkpoints.
```

---

## 38. Error & Log Debugging

### Prologue Gate Failures
```
@[systematic-debugging] @[verification-before-completion] @[context-fundamentals]
Leo.py is failing silently at Prologue P1 (League/Team Threshold Check). Pull all relevant logs, trace the exact execution path from Leo.py startup through Core/System/ data readiness checker, identify which leagues are below 90% coverage threshold and why auto-remediation did not trigger. Produce: root cause, exact file/line, minimal fix, and a log improvement patch so this never fails silently again.
```

### Chapter 1 Odds Harvesting Errors
```
@[systematic-debugging] @[webapp-testing] @[aigo-self-healing]
Chapter 1 P1 (URL Resolution & Odds Harvesting from Football.com) is producing incomplete odds for certain markets. Trace the full Playwright execution in Modules/FootballCom/ — capture screenshots at each step, log every selector hit/miss, identify whether the failure is a timing issue, selector change, or network timeout. Output: ranked list of failure causes, selector patches, and a retry strategy with exponential backoff.
```

### RL Adapter Readiness Crash
```
@[systematic-debugging] @[evaluation] @[context-degradation]
Leo.py crashes at Prologue P3 (RL Adapter Readiness Check) for specific leagues. Trace Core/Intelligence/rl/ adapter loading sequence — identify which leagues have missing/corrupt LoRA adapter files, why the auto-remediation training trigger failed, and whether the SharedTrunk weights are compatible with the adapter checkpoint versions. Fix the loading logic and add adapter integrity validation on startup.
```

### Supabase Sync Errors
```
@[systematic-debugging] @[verification-before-completion] @[filesystem-context]
The bi-directional cloud sync (Leo.py --sync Bootstrap) is producing data parity errors between local SQLite (Data/Store/leobook.db) and Supabase. Analyze the sync logs, identify: which tables are drifting, whether the conflict resolution strategy favors local or cloud incorrectly, and any missing upsert logic. Produce a sync audit report and a patched sync function with proper conflict resolution.
```

### Live Streamer Silent Death
```
@[systematic-debugging] @[context-fundamentals] @[bdi-mental-states]
The Live Streamer (Modules/Flashscore/) is dying silently mid-session without logs. Instrument every async operation with structured logging (timestamp, event type, match_id, error), add a heartbeat ping every 60s to Supabase so death is detectable externally, trace why Bootstrap Sync wait condition is sometimes never resolved, and add a watchdog that auto-restarts the streamer on unexpected exit.
```

### Gemini Key Rotation Failures
```
@[systematic-debugging] @[context-optimization] @[verification-before-completion]
LeoBook's 25+ Gemini key rotation is hitting quota errors without gracefully rotating to the next key. Trace Scripts/build_search_dict.py and all LLM call sites — log which key failed, at what quota limit, and whether the rotation index is persisted across restarts. Fix: implement a key health registry in Supabase, skip exhausted keys automatically, alert when fewer than 5 keys remain healthy.
```

### Prediction Pipeline Wrong Output
```
@[advanced-evaluation] @[systematic-debugging] @[receiving-code-review]
Chapter 1 P2 (Rule Engine + Neural RL Ensemble) is producing predictions that contradict historical base rates for certain leagues. Log the full ensemble decision for 10 recent matches — capture Rule Engine confidence, RL policy entropy, ensemble weighting, and final output. Compare against actual outcomes. Identify whether the bug is in feature construction, reward shaping, or ensemble aggregation logic.
```

### Scheduler Drift & Missed Tasks
```
@[systematic-debugging] @[test-driven-development] @[verification-before-completion]
Core/System/scheduler.py is missing the Monday 2:26am Weekly Enrichment window after server restarts. Trace the task queue persistence mechanism — does the scheduler recover pending tasks from Supabase on startup? Identify all cases where task timestamps drift due to timezone handling in now_ng utilities. Patch, add a missed-task recovery test, and verify with a simulated restart scenario.
```

---

## 39. Rulebook First Principles

### Apply RULEBOOK to New Feature Design
```
@[brainstorming] @[writing-plans] @[doc-coauthoring]
Before implementing any new feature in LeoBook, apply RULEBOOK.md first principles systematically. For the proposed feature [INSERT FEATURE], answer: (1) Does it violate the 1-Prediction-Per-Team-Per-Week constraint? (2) Does it respect Data Readiness Gate sequencing? (3) Does it touch the Standings VIEW or require a new table? (4) Is it additive or does it mutate existing pipeline state? Produce a first-principles compliance checklist before writing a single line of code.
```

### RULEBOOK Violation Audit
```
@[requesting-code-review] @[systematic-debugging] @[context-fundamentals]
Perform a full RULEBOOK.md compliance audit across the entire LeoBook codebase. For every engineering decision documented in RULEBOOK.md, verify whether the actual code respects it. Flag every violation with: file, line, rule broken, severity (critical/minor), and recommended fix. Output as a structured audit report sorted by severity.
```

### First Principles Refactor Decision
```
@[bdi-mental-states] @[project-development] @[writing-plans]
Apply RULEBOOK.md first-principles thinking to decide whether [INSERT SYSTEM e.g. computed_standings VIEW / Task Scheduler / RL adapter loading] should be refactored or rebuilt from scratch. Use the framework: (1) What problem does it currently solve? (2) What constraints does RULEBOOK impose? (3) What is the minimum change that respects all constraints? (4) What would a rebuild gain vs lose? Produce a reasoned engineering decision document.
```

### RULEBOOK as Agent Constitution
```
@[skill-creator] @[writing-skills] @[bdi-mental-states]
Convert RULEBOOK.md into a formal agent constitution for LeoBook. Structure it as: core invariants (never violate), soft guidelines (prefer but can override with justification), deprecated decisions (no longer apply), and open questions (not yet decided). Then create a custom @[leobook-rules] skill that any agent must invoke before touching Leo.py, Core/, or Modules/.
```

### Engineering Decision Log
```
@[doc-coauthoring] @[writing-plans] @[internal-comms]
Create a formal Architecture Decision Record (ADR) system for LeoBook based on RULEBOOK.md. Retroactively document the 10 most consequential v7.0 decisions as ADRs: context, decision, consequences, and status. Going forward, every architectural change must produce an ADR before implementation. Store ADRs in a new docs/decisions/ directory and link from RULEBOOK.md.
```

---

## 40. Modular Evaluation — Prologue

### Prologue Full Evaluation
```
@[evaluation] @[filesystem-context] @[systematic-debugging]
Perform a complete evaluation of LeoBook's Prologue (P1-P3 Data Readiness Gates). Map every file involved: which modules are imported, which DB tables are queried, which thresholds are checked, which auto-remediation scripts are triggered. Trace a full execution from Leo.py startup through P1 → P2 → P3 with sample data. Identify: average runtime, most common failure point, remediation success rate, and 3 specific improvements.
```

### Prologue P1 Deep Evaluation
```
@[evaluation] @[advanced-evaluation] @[test-driven-development]
Deep evaluate Prologue P1 (League/Team Threshold Check, 90% Coverage). List every file and function involved. Trace: how coverage % is calculated, what counts as a covered team, how the 90% threshold was chosen and whether it's optimal, what happens at exactly 89% vs 91%, and how P1 failure triggers enrichment. Write 10 edge-case unit tests that P1 must pass before any prediction cycle runs.
```

### Prologue P2 Deep Evaluation
```
@[evaluation] @[systematic-debugging] @[verification-before-completion]
Deep evaluate Prologue P2 (Historical Seasons Check, 2+ Seasons). Map all files in Data/Access/ and Scripts/ involved. Trace: how seasons are counted per league, what constitutes a "valid" season (minimum matches?), how P2 interacts with the enrichment pipeline, and what happens when a newly promoted league has only 1 season of data. Identify the 3 most likely P2 failure scenarios and fix each.
```

### Prologue P3 Deep Evaluation
```
@[evaluation] @[project-development] @[bdi-mental-states]
Deep evaluate Prologue P3 (RL Adapter Readiness Check). Map every file in Core/Intelligence/rl/ involved. Trace: how adapter readiness is determined (file existence? checksum? minimum training epochs?), how the SharedTrunk compatibility is verified, what triggers auto-training, and how long P3 remediation takes for a cold league. Define a formal readiness contract that P3 must enforce and implement missing validations.
```

---

## 41. Modular Evaluation — Startup

### Startup Full Evaluation
```
@[evaluation] @[filesystem-context] @[systematic-debugging]
Evaluate LeoBook's full Startup sequence (DB Parity → Bi-directional Cloud Sync Bootstrap). Map every file, function, and DB operation involved from the moment Leo.py is invoked to when the Task Scheduler begins. Measure: startup time under normal conditions, startup time after 7-day offline period, failure modes if Supabase is unreachable, and whether startup is idempotent (safe to run twice). Produce a startup health scorecard.
```

### Startup DB Parity Deep Evaluation
```
@[systematic-debugging] @[test-driven-development] @[verification-before-completion]
Deep evaluate the DB Parity enforcement during LeoBook startup. Trace every table compared between SQLite and Supabase, how conflicts are resolved (last-write-wins? cloud-wins?), what happens if a table exists locally but not in cloud, and whether schema migrations are handled automatically. Write tests for: fresh install, partial sync, corrupted local DB, and cloud-ahead-of-local scenarios.
```

---

## 42. Modular Evaluation — Chapter 1

### Chapter 1 Full Evaluation
```
@[evaluation] @[advanced-evaluation] @[filesystem-context]
Perform a complete evaluation of Chapter 1 (Prediction Pipeline: P1 → P2 → P3). Map every file, function, DB query, and external API call involved. Trace a single match from URL resolution through odds harvesting → rule engine → RL inference → ensemble → recommendation → sync. Measure: total latency per match, Gemini API calls consumed, Supabase writes performed, and failure recovery at each sub-step.
```

### Chapter 1 P1 Deep Evaluation
```
@[evaluation] @[webapp-testing] @[systematic-debugging]
Deep evaluate Chapter 1 P1 (URL Resolution & Odds Harvesting from Football.com). Map every Playwright function in Modules/FootballCom/ involved. Trace: how match URLs are resolved from the schedule, how odds are scraped per market (1X2, BTTS, Over/Under etc.), what happens when a market is unavailable, and how harvested odds are stored. Profile selector fragility — which selectors are most likely to break on a Football.com UI update.
```

### Chapter 1 P2 Deep Evaluation
```
@[evaluation] @[advanced-evaluation] @[bdi-mental-states]
Deep evaluate Chapter 1 P2 (Rule Engine + Neural RL Ensemble + Smart Scheduling). Map every file in Core/Intelligence/ involved. Trace: how the Rule Engine generates its prediction and confidence score, how the RL model produces its policy distribution, how the ensemble combines both, and how Smart Scheduling enforces the 1-prediction-per-team-per-week constraint. Identify where these systems can disagree and how conflicts are resolved.
```

### Chapter 1 P3 Deep Evaluation
```
@[evaluation] @[doc-coauthoring] @[verification-before-completion]
Deep evaluate Chapter 1 P3 (Recommendations & Final Chapter Sync). Map every file in Scripts/recommend_bets.py and Data/Access/ involved. Trace: how recommendations are ranked from raw predictions, what filters are applied (confidence threshold, market availability, team constraint), how the final sync writes to Supabase, and what the rollback strategy is if sync fails mid-write. Identify data integrity risks and fix each.
```

---

## 43. Modular Evaluation — Chapter 2

### Chapter 2 Full Evaluation
```
@[evaluation] @[webapp-testing] @[systematic-debugging]
Perform a complete evaluation of Chapter 2 (Betting Automation: P1 → P2). Map every file, Playwright action, DB state change, and Supabase write involved. Trace a single bet from recommendation selection → Football.com login → odds confirmation → bet placement → funds check → withdrawal logic. Measure: average placement time, failure rate per step, and what happens when odds drift between harvest and placement.
```

### Chapter 2 P1 Deep Evaluation
```
@[evaluation] @[webapp-testing] @[test-driven-development]
Deep evaluate Chapter 2 P1 (Automated Booking on Football.com). Map every Playwright function in Modules/FootballCom/ involved. Trace: login session management (does it reuse cookies?), odds confirmation before booking (does it detect line movement?), bet slip construction for combo bets, and error handling for rejected bets. Write tests for: session expiry mid-booking, odds changed alert, insufficient funds, and network timeout at confirmation.
```

### Chapter 2 P2 Deep Evaluation
```
@[evaluation] @[systematic-debugging] @[verification-before-completion]
Deep evaluate Chapter 2 P2 (Funds & Withdrawal Check). Map every file in Core/System/ withdrawal logic involved. Trace: how balance is read from Football.com, what threshold triggers withdrawal, how withdrawal is executed via Playwright, and how the result is recorded in Supabase. Identify: what happens if withdrawal fails, whether partial withdrawals are handled, and whether the funds check is blocking or non-blocking relative to Chapter 2 P1.
```

---

## 44. Modular Evaluation — Live Streamer

### Streamer Full Evaluation
```
@[evaluation] @[systematic-debugging] @[filesystem-context]
Perform a complete evaluation of the Live Streamer (Modules/Flashscore/ isolated parallel task). Map every file, async loop, Supabase subscription, and state machine involved. Trace: how it waits for Bootstrap Sync, how live scores are fetched and written, how Outcome Review is triggered on match completion, how the Accuracy Report is generated, and how it handles simultaneous matches across 20+ leagues. Identify memory leak risks in long-running sessions.
```

### Streamer Outcome Review Evaluation
```
@[evaluation] @[advanced-evaluation] @[test-driven-development]
Deep evaluate the Outcome Review subsystem within the Live Streamer. Trace: how finished matches are detected, how actual results are compared to LeoBook's predictions, how accuracy is computed per market and per league, how results are written to Supabase, and how the Flutter app renders the accuracy report in real-time. Write tests for: draws, abandoned matches, postponed matches, and multi-outcome combo bet evaluation.
```

### Streamer Accuracy Report Evaluation
```
@[evaluation] @[doc-coauthoring] @[advanced-evaluation]
Deep evaluate the Accuracy Report generation in the Live Streamer. Trace: what metrics are computed (per-market accuracy, ROI, hit rate, streak data), how historical accuracy is aggregated vs session accuracy, how the report is formatted and stored, and how the Flutter 4-tab match system surfaces it. Design an enhanced accuracy report schema that supports: per-league breakdown, RL-vs-RuleEngine contribution, and weekly/monthly trend lines.
```

---

## 45. Modular Evaluation — Cross-Cutting

### now_ng Utilities Full Evaluation
```
@[evaluation] @[systematic-debugging] @[test-driven-development]
Evaluate every usage of Core/Utils/now_ng utilities across the entire LeoBook codebase. List every file that imports it, what timezone assumption each call makes, whether DST transitions are handled, and whether now_ng is consistent with Supabase's UTC timestamps and Flashscore's local match times. Write a timezone correctness test suite that runs against real match data from at least 5 different timezone regions.
```

### Config Layer Full Evaluation
```
@[evaluation] @[filesystem-context] @[doc-coauthoring]
Evaluate LeoBook's Config/ directory. Map every config value, which module consumes it, whether it can be overridden by environment variables, and which values are currently hardcoded in Leo.py or other modules instead of being in Config/. Produce a config consolidation plan that moves all magic numbers, thresholds (90% coverage, 2+ seasons, 6h cycle wait), and market lists into a single typed config schema.
```

### Data Flow Full Evaluation
```
@[evaluation] @[context-optimization] @[writing-plans]
Trace LeoBook's complete data flow from raw extraction to bet placement. For each data entity (match, league, team, prediction, odds, bet, outcome), map: where it enters the system, every transformation it undergoes, every DB write it triggers, every Supabase sync it participates in, and where it is finally consumed by the Flutter app. Identify redundant transformations, missing validation steps, and data entities that are never cleaned up.
```

---

## 46. End-to-End Latency Evaluation

```
@[evaluation] @[context-optimization] @[advanced-evaluation]
Instrument LeoBook's full autonomous cycle (Startup → Prologue → Chapter 1 → Chapter 2 → Streamer) with millisecond-precision timing at every major step. Produce a latency breakdown: what % of total cycle time is Playwright, Gemini API, Supabase sync, RL inference, and SQLite operations. Identify the single highest-impact optimization and implement it with before/after benchmark comparison.
```

---

*LeoBook Antigravity Skill Prompts — v1.0*
*Generated: March 2026 · Materialless LLC · Chief Engineer: Emenike Chinenye James*
